import React from "react";
// import PropTypes from "prop-types";

const Comments = () => (
  <div>
    <hr />
    <h2>Comments</h2>
    <p>No comments yet.</p>
  </div>
);

// Comments.propTypes = {
//   tags: PropTypes.array
// };

export default Comments;
